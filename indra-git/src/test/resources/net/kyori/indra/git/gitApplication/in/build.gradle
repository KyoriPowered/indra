plugins {
  id 'net.kyori.indra.git'
}

def git = indraGit.git()

task printGitStatus {
  inputs.property('git', git).optional(true)
  doLast {
    logger.lifecycle("Git present: ${git.isPresent()}")
  }
}
