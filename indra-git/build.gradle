dependencies {
  compileOnlyApi "org.jetbrains:annotations:23.0.0"
  api "org.eclipse.jgit:org.eclipse.jgit:5.11.0.+"
  api "net.kyori:mammoth:$mammothVersion"
  testImplementation "net.kyori:mammoth-test:$mammothVersion"
  // testImplementation testFixtures(project(":indra-common"))
}

indraPluginPublishing {
  plugin(
    "indra.git",
    "net.kyori.indra.git.GitPlugin",
    "Indra Git",
    "Expose the Git repository that may contain the active project",
    ["vcs", "git", "indra"]
  )
}
